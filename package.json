{
  "name": "web-push-test",
  "private": true,
  "version": "0.0.1",
  "description": "web push test",
  "main": "sw.js",
  "scripts": {
    "build": "npm-run-all --parallel build:**",
    "build:browser": "tsc -p browser/tsconfig.json",
    "build:sw": "tsc -p sw/tsconfig.json",
    "start": "http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "git+ssh://git@github.com/k-mats/web-push-test.git",
  "author": "k-mats",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/k-mats/web-push-test/issues"
  },
  "homepage": "https://github.com/k-mats/web-push-test#readme",
  "dependencies": {
    "typescript": "^3.3.4000"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "npm-run-all": "^4.1.5"
  }
}
